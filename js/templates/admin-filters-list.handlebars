<div class="table-responsive">
    <table class="table dataTable table-responsive table-hover">
        <thead class="thead-inverse">
            <tr>
                <th> </th>
                {{#each this}}
                    {{#if @first}}
                        {{#each this}}
                            <th>{{@key}}</th>
                        {{/each}}
                    {{/if}}
                {{/each}}
                <th> </th>
            </tr>
        </thead>
        <tbody>
            {{#each this}}
                <tr>
                    <td><button data-toggle="modal" data-target="#edit-filter-modal" onclick="adminController.editFilter('{{this.id}}')">Edit</button></td>
                    {{#each this}}
                        <td>{{this}}</td>
                    {{/each}}
                    <td><button onclick="adminController.deleteFilter('{{this.id}}')">x</button></td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>

<div id="edit-filter-modal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
 <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
        <h4 class="modal-title" id="myModalLabel">Edit Filter:</h4>
      </div>
      <div id="edit-filter-modal-body" class="modal-body">

      </div>
      <div class="modal-footer">
        <button onclick="adminController.sendEditedFilter()" type="button" class="btn btn-primary">Save changes</button>
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>
